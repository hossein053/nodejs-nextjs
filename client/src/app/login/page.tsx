import { LoginPage } from '@/components'
import React, { Fragment } from 'react'

export default async function Page() {
    return (
        <Fragment>
            <LoginPage />
        </Fragment>
    )
}
